# Unify Core Program (Skeleton)

This folder contains an Anchor-compatible starter program for Unify platform primitives.

Initial scope:
- `initialize_platform` instruction
- `PlatformConfig` PDA account (`seeds = ["platform"]`)

This is a scaffold only and is intentionally minimal. Expand instruction set and account models once
program-level requirements are finalized.
